# Global overrides 
nameOverride: ""
fullnameOverride: ""

replicaCount: 1

# Deployment configuration
image: 
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent 
  containerPort: 80 

# Service configuration
service:
  type: ClusterIP 
  port: 80 
  